__pycache__/
*.pyc
.DS_Store

# Raw daily data changes every run — not tracked, regenerate with day01 script
data/*.csv

# Engineered feature files also regenerate every run — not tracked, regenerate with day03 script
data/features/*.csv

# News headlines + sentiment calls also regenerate every run — not tracked, regenerate with day08 script
data/news/*.json

# Daily reports also regenerate every run — not tracked, regenerate with day07 script
outputs/reports/*.md

# Chart images ARE tracked (outputs/charts/) — they're the visible portfolio evidence
